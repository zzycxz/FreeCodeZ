interface:
  display_name: Architecture Governance
  short_description: Check module boundaries and prepare a bounded code context
  default_prompt: Use $architecture-governance before editing code to inspect the affected module, generate its context package, and run the architecture checks before and after the change.
